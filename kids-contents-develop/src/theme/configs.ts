import { ThemeConfig } from "antd";

export const configuration: ThemeConfig = {
  token: {
    colorPrimary: "#ACBF01",
       fontFamily:
      'var(--font-main), "Montserrat", "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif',

  },
};
